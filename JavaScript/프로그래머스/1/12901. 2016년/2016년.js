function solution(a, b) {
    // var answer = '';
    // return answer;
    // console.log(new Date(2016, a - 1, b).toString().slice(0, 3).toUpperCase());
    return new Date(2016, a - 1, b).toString().slice(0, 3).toUpperCase();
}